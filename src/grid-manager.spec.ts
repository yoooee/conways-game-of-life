import { GridManager } from './grid-manager';
import { mockGridMarkup } from './test-helper';

//GridManager.update(currentPattern);
//Update takes the current Pattern, checks each cell, and returns a new pattern.


describe('GridManager', () => {
  describe ('update', () => {
    // Checkes each cell of the grid and updates its status.
    it('reloads the grid with a new pattern', () => {

    });
  });
});
