import { Cell } from './cell';

describe('Cell', () => {
  describe('GetActiveNeighbors', () => {
// Should check nw, n, ne, e, se, s, sw, w, nw for active neighbors.
    
    // Updates cell status accordingly.
  });
})
