import { Game } from './game';

describe('Game', () => {

  // This test should be in index.
  //let subject;
  //let rowCount: number = 10;
  //let cellCount: number = 10;

  //describe('Initialize', () => {

    //beforeEach(() => {
      //subject = new Game(rowCount, cellCount);
    //})
    //it('generates a 10 x 10 grid', () => {

      //expect(subject.numberOfRows).toEqual(rowCount);
      //expect(subject.numberOfCells).toEqual(cellCount);

    //});
  //});
});
